<Window x:Class="AppLauncher.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Application" 
        Height="160" Width="340"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <Border CornerRadius="16"
            Background="#EE202024"
            BorderBrush="#35FFFFFF"
            BorderThickness="1"
            Padding="18">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="6" Direction="270" Opacity="0.6" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText"
                       Grid.Row="0"
                       Text="✏️ 重命名应用显示名称"
                       Foreground="#E0FFFFFF"
                       FontSize="13"
                       FontWeight="SemiBold"
                       FontFamily="Segoe UI Variable Display, Microsoft YaHei UI, sans-serif"/>

            <Border Grid.Row="1"
                    Margin="0,10,0,12"
                    Height="32"
                    CornerRadius="8"
                    Background="#25FFFFFF"
                    BorderBrush="#30FFFFFF"
                    BorderThickness="1">
                <TextBox x:Name="NameInput"
                         Foreground="#FFFFFFFF"
                         Background="Transparent"
                         BorderThickness="0"
                         FontSize="12.5"
                         VerticalContentAlignment="Center"
                         Margin="8,0"
                         CaretBrush="#FF0A84FF"
                         KeyDown="NameInput_KeyDown"/>
            </Border>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CancelBtn"
                        Content="取消" 
                        Width="68" Height="26"
                        Foreground="#B0FFFFFF"
                        Background="#20FFFFFF"
                        BorderBrush="#30FFFFFF"
                        BorderThickness="1"
                        FontSize="11.5"
                        Margin="0,0,8,0"
                        Cursor="Hand"
                        Click="CancelButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="7" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button x:Name="OkBtn"
                        Content="确定" 
                        Width="68" Height="26"
                        Foreground="#FFFFFFFF"
                        Background="#FF0A84FF"
                        BorderThickness="0"
                        FontSize="11.5"
                        Cursor="Hand"
                        Click="OkButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="7" Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
