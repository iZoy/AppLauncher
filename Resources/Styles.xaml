<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:services="clr-namespace:AppLauncher.Services">

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    <services:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    <services:NullToVisibilityConverter x:Key="NotNullToVisibilityConverter" CollapseWhenNull="true"/>
    <services:NullToVisibilityConverter x:Key="NullToVisibleConverter" CollapseWhenNull="false"/>

    <!-- Modern Fluent Style ContextMenu -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="#E51E1E22"/>
        <Setter Property="BorderBrush" Value="#35FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="#FFFFFFFF"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="StaysOpen" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="6" Direction="270" Opacity="0.5" Color="#000000"/>
                        </Border.Effect>
                        <StackPanel IsItemsHost="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Modern Fluent Style MenuItem -->
    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="#F2FFFFFF"/>
        <Setter Property="FontSize" Value="13.5"/>
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Microsoft YaHei UI, sans-serif"/>
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="MenuItemBorder"
                            CornerRadius="7"
                            Background="Transparent"
                            Margin="2,1"
                            Padding="{TemplateBinding Padding}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="22"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <ContentPresenter ContentSource="Icon" 
                                              HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"
                                              Width="16" Height="16"/>

                            <ContentPresenter Grid.Column="1" 
                                              ContentSource="Header" 
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"
                                              Margin="6,0,0,0"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="MenuItemBorder" Property="Background" Value="#30FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="#25FFFFFF"/>
        <Setter Property="Margin" Value="4,4"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1" Background="{TemplateBinding Background}" Margin="{TemplateBinding Margin}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Minimal auto-hiding scrollbar with a subtle hover highlight -->
    <Style TargetType="ScrollBar">
        <Setter Property="Width" Value="14"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="Transparent">
                        <Track x:Name="PART_Track" IsDirectionReversed="true">
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border x:Name="ThumbBorder"
                                                    CornerRadius="4"
                                                    Width="8"
                                                    HorizontalAlignment="Right"
                                                    Background="#80FFFFFF"
                                                    Margin="0,3,3,3">
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <!-- Pure White Highlight and Expanded Width when Mouse Over or Dragging -->
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ThumbBorder" Property="Background" Value="#FFFFFFFF"/>
                                                    <Setter TargetName="ThumbBorder" Property="Width" Value="10"/>
                                                    <Setter TargetName="ThumbBorder" Property="CornerRadius" Value="5"/>
                                                </Trigger>
                                                <Trigger Property="IsDragging" Value="True">
                                                    <Setter TargetName="ThumbBorder" Property="Background" Value="#FFFFFFFF"/>
                                                    <Setter TargetName="ThumbBorder" Property="Width" Value="10"/>
                                                    <Setter TargetName="ThumbBorder" Property="CornerRadius" Value="5"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
